/**
 * 
 * @param {number} a 
 * @param {number} b 
 * @returns 
 */
module.exports.sum = function(a, b) {
  return a + b;
}

/**
 * 
 * @param {number} a 
 * @param {number} b 
 * @returns 
 */
module.exports.multiply = function(a, b) {
  return a * b;
}
